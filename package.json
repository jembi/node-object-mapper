{
  "author": "Jembi health Systems NPC",
  "name": "object-mapper",
  "description": "Map fields from one object to another using a user defined schema",
  "version": "0.1.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/jembi/node-object-mapper.git"
  },
  "scripts": {
    "test": "NODE_ENV=test tape test/*.js"
  },
  "dependencies": {
    "minimatch": "^3.0.4"
  },
  "devDependencies": {
    "debug": "^2.2.0",
    "tape": "^4.11.0"
  },
  "engines": {
    "node": "*"
  }
}
